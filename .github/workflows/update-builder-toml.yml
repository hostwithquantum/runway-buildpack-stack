name: Update builder.toml and Send Pull Request

on:
  schedule:
  - cron: '36 0,12 * * *' # daily at 00:36 and 12:36 UTC
  workflow_dispatch: {}

concurrency: builder_update

jobs:
  update:
    name: Update builder.toml
    runs-on: ubuntu-22.04
    steps:
    - id: generate-token
      uses: actions/create-github-app-token@v3
      with:
        client-id: ${{ secrets.RUNWAY_CI_BOT_APP_ID }}
        private-key: ${{ secrets.RUNWAY_CI_BOT_PRIVATE_KEY }}
    - uses: actions/checkout@v6
      with:
        token: ${{ steps.generate-token.outputs.token }}
        persist-credentials: true

    - name: Checkout branch
      uses: paketo-buildpacks/github-config/actions/pull-request/checkout-branch@main
      with:
        branch: "automation/builder-toml"

    - uses: docker/login-action@v4
      with:
        username: ${{ secrets.DOCKERHUB_REGISTRY_USERNAME }}
        password: ${{ secrets.DOCKERHUB_REGISTRY_PASSWORD }}

    - name: Update builder.toml
      uses: paketo-buildpacks/github-config/actions/builder/update@main
      with:
        filename: builders/jammy/builder.toml
        token: ${{ steps.generate-token.outputs.token }}

    - name: Git commit
      id: commit
      uses: paketo-buildpacks/github-config/actions/pull-request/create-commit@main
      with:
        message: "Update builder.toml"
        pathspec: "builders/jammy/builder.toml"
        committer_name: ${{ steps.generate-token.outputs.app-slug }}[bot]
        committer_email: ${{ steps.generate-token.outputs.user-id }}+${{ steps.generate-token.outputs.app-slug }}[bot]@users.noreply.github.com

    - name: Git push
      if: ${{ steps.commit.outputs.commit_sha != '' }}
      uses: paketo-buildpacks/github-config/actions/pull-request/push-branch@main
      with:
        branch: "automation/builder-toml"

    - name: Open Pull Request
      if: ${{ steps.commit.outputs.commit_sha != '' }}
      uses: paketo-buildpacks/github-config/actions/pull-request/open@main
      with:
        token: ${{ steps.generate-token.outputs.token }}
        title: "Updating builder.toml"
        branch: "automation/builder-toml"
